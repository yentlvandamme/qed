# QED
The Quick EDit tool for inline text editing.

QED allows you to append, insert, or replace a line of text in a file by line number.

```
Example usage:
<text to insert> <target filename> <line number> <verb>
"Foo" text1.txt 3 Replace
```